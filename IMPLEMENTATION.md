# 公考研习 · 第一版实现

## 产品规则

- 桌面网页，最小内容宽度 1100px，不做手机适配。
- 每页一题，四选一；选择即保存，可跳过、前后翻页、答题卡跳转和标记。
- 全部答完才能交卷，客户端按钮与服务端均校验。交卷后不可修改。
- 首次正确：答案历史只有一次有效选择且正确。修改正确：最终正确但发生过改选。其余为最终错误。重复点击当前答案不构成改选。
- 首次正确率、修改正确率均以本次总题数为分母；没有“已答正确率”。
- 修改正确和最终错误自动加入个人错题集。首次正确不新增错题；后续首次答对仍保留先前错题历史。
- 题目停留时间在后台累加，仅在结果分析展示。后台标签页暂停；计时心跳为 5 秒，突发浏览器休眠/卡顿超过 15 秒不全量计入。

## 技术结构

- React / TypeScript / Vinext，Cloudflare Worker + D1，原始题库文件只用于解析，不保存上传原件。
- `lib/domain.ts`：判分、事件与题库校验。
- `lib/draft.ts`：本地待同步队列、恢复、重试、冲突合并。
- `lib/server.ts` 与 `app/api/[...path]/route.ts`：身份鉴权、权限、存储、判分与题库管理。
- `lib/importer.ts`：Excel / Markdown / Word 文本解析。
- `components/`：桌面答题、结果、错题与管理页面。
- `.wrangler/state`：本地预览数据库；`.wrangler/test-state`：隔离的接口测试数据库。两者均不提交。
- 每次练习保存题目及答案快照，题库后续编辑不改变历史报告。
- 同步事件带唯一编号；服务端使用乐观版本号和原子 D1 batch，防止重试重复入账或并发覆盖。错题从已交卷快照归集，避免双写导致不一致。
- 同浏览器使用 Web Locks 防止同一练习在多个标签页重复计时。跨设备的操作通过版本冲突合并；同一练习建议只在一台设备操作。

## 登录与权限

登录使用 Sites 平台的 ChatGPT 身份，应用在服务端维护 admin / user 角色。新站点保持仅站点所有者可访问，首次登录者初始化为管理员。后续用户须由管理员登记对应登录邮箱。

在线多人使用需要同时满足：平台站点允许该访问者访问；应用内有该邮箱的用户登记。应用内“添加用户”不发送邮件，也不自动扩大站点访问范围。

本地开发使用 starter 提供的环回开发登录 `seedy@sites.test`，生产构建不包含本地模拟登录。接口测试向隔离的本地 Worker 注入平台身份头，生产环境的身份头由平台网关提供，不可使用裸 Worker 暴露部署而跳过身份验证网关。

## 导入约定

- 支持 `.xlsx` / `.xls` / UTF-8 `.md` / `.docx` / `.doc`。
- Excel 表头：题号、题干、A、B、C、D、正确答案、解析、分类、材料；多个工作表逐一读取。
- Word 与 Markdown：题号开头，选项逐行，以 `答案：B`、`解析：...` 等标识对应内容。标准模板见 `public/templates/questions.md`，管理页可下载 Excel 模板。
- 单个文件最大 8MB，一次最多 300 题。先预览和修正，再保存。缺题干、缺选项、多答案、重复题号、重复题干均阻止保存。
- Word 按文字读取；图片、复杂公式及特殊布局不自动保留。导入页明确提示核对。
- Excel 解析使用 [SheetJS 官方分发版本](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/)，Word 使用 mammoth 与 [word-extractor](https://github.com/morungos/node-word-extractor)。

## 验证

`node --test tests/core.test.mjs tests/draft.test.mjs` 验证互斥判分、重复点击、计时增量与离线重试。

`node tests/integration.mjs` 对 `127.0.0.1:8788` 的隔离测试 Worker 验证权限、完整交卷、并发同步、幂等重试、历史快照、错题重做与五种文件扩展名。

已执行 TypeScript 检查和生产构建。未执行浏览器点击或截图验收。WebMCP 只注册一个读取练习概况的工具，环境无可用验证上下文，因此未宣称验证通过。
